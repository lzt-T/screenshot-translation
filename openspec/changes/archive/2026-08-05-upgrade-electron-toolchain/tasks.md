## 1. 依赖基线与升级

- [x] 1.1 确认 pnpm 和 Node.js 满足目标工具链的 engines 要求，记录当前解析到的 Electron 技术栈，并在修改依赖前核对工作区状态。
- [x] 1.2 将 Electron、electron-vite、Vite、React Vite 插件、electron-builder、electron-updater、Electron Toolkit tsconfig 和 Node.js 类型更新到设计中的兼容目标矩阵，然后刷新 pnpm 锁文件且不修改无关依赖。
- [x] 1.3 将 better-sqlite3 升级到 13.0.3，作为独立诊断步骤，并通过现有 pnpm/postinstall 流程针对 Electron 43 重新构建应用原生依赖。
- [x] 1.4 验证最终依赖清单和锁文件解析到预期版本，保持现有 caret 版本范围约定，且不包含非预期的依赖升级。

## 2. 构建配置兼容性

- [x] 2.1 移除已废弃的 electron-vite `externalizeDepsPlugin` 导入和主进程注册，改用 electron-vite 5 的默认依赖外部化行为。
- [x] 2.2 仅解决所选升级引起的类型、配置或 Electron API 不兼容，同时保持现有入口、路径别名、输出路径、IPC 契约和应用行为不变。开发测试暴露截图窗口启动回归后曾重新打开此任务；打包测试发现 AudioWorklet 模块加载回归后再次重新打开。
- [x] 2.3 确认现有 builder 配置仍保持应用标识、资源包含规则、ASAR 解包规则、原生依赖准备、构建目标、制品名称和 generic 更新 provider 不变。

## 3. 验证与打包

- [x] 3.1 在明确授权执行现有 package script 后，运行项目的 typecheck 和生产构建，并仅解决升级引起的失败；不新增或执行单元测试。
- [x] 3.2 构建未打包的 Windows 应用，确认其能够启动，并检查打包输出中的 better-sqlite3、tesseract、sherpa-onnx、应用资源、训练数据文件和 Drizzle 迁移。
- [x] 3.3 对未打包应用执行冒烟测试，覆盖主窗口、截图选择与翻译、多显示器坐标、全局快捷键、通知、剪贴板复制、SQLite 持久化记录、语音识别和语音合成。
- [x] 3.4 构建并安装 Windows NSIS 制品，然后在具备安全测试更新时验证应用启动、现有用户数据、更新检查、更新下载进度和重启安装行为。
- [x] 3.5 执行可用的 Linux Wayland 和已签名 macOS 通知检查；合适的构建环境不可用时，将对应平台检查记录为未验证。

## 4. 截图窗口回归

- [x] 4.1 在开发环境复现 F2 失败，并确认是截图窗口构造还是 renderer 加载阻止遮罩显示。
- [x] 4.2 在开发和生产环境加载专用截图选择器 renderer 入口，以显式的当前显示器 bounds 隐藏创建窗口，并仅在 renderer 成功加载后显示窗口。
- [x] 4.3 将截图启动改为异步；构造或加载失败时关闭已创建的部分资源、停止光标跟踪、重置截图状态和 bounds，并恢复主窗口及其焦点。
- [x] 4.4 在开发环境复测初始启动修正并记录结果：遮罩已显示，但鼠标拖拽和 Escape 取消仍不可用。
- [x] 4.5 恢复每 150ms 检查光标所在显示器的等待选择逻辑，仅在 `display.id` 变化时更新截图窗口 bounds 并重新置顶、聚焦；第一次原生 `mouseDown` 到达后停止跟踪并锁定当前显示器。
- [x] 4.6 集中处理截图取消，并添加主进程 `before-input-event` Escape 兜底：关闭遮罩、停止跟踪、清除 bounds 并重置截图状态，不再仅依赖 renderer 键盘事件。
- [x] 4.7 记录第二次开发环境复测结果：遮罩层可以显示，但仍无法开始拖拽选区；删除显示器跟踪没有解决输入问题，并破坏了多显示器间的光标跟随能力。
- [x] 4.8 为截图窗口增加覆盖全区域的极低透明度鼠标命中层，并在开发模式通过 `before-mouse-event` 记录第一次原生 `mouseDown` 是否到达，以区分原生命中问题和 renderer 事件链问题。
- [x] 4.9 记录第三次开发环境复测结果：仍无法开始拖拽选区，但主进程已输出 `[ScreenshotWindow] Native mouseDown received`，确认原生鼠标事件到达 Chromium，问题位于 renderer 事件链。
- [x] 4.10 将截图选择器改为容器 Pointer Events：`pointerdown` 先建立绘制状态并捕获指针，使用 Ref 保存起点、绘制状态和最新选区，`pointermove` 更新选区，`pointerup` 使用最新选区完成选择并释放 Pointer Capture。
- [x] 4.11 删除不再需要的 `START_SCREENSHOT` renderer 发送、主进程监听和 IPC 枚举，由现有 `before-mouse-event` 统一负责第一次按下时停止多显示器跟踪。
- [x] 4.12 记录第四次开发环境复测结果：已能绘制截图选区，但松开鼠标后遮罩层仍然停留，截图捕获、翻译和结果展示均未开始，确认问题位于 `SCREENSHOT_SELECTED` renderer IPC 边界。
- [x] 4.13 为截图窗口和结果窗口显式设置 `sandbox: false`，保留 `contextIsolation: true` 和现有共享 preload，使辅助窗口与主窗口使用一致的 IPC bridge 运行环境。
- [x] 4.14 记录第五次开发环境复测结果：同屏截图已经可以完成选区、关闭遮罩、捕获、翻译并展示结果窗口，但“显示原图”只有轻微背景透明度变化且仍显示译文块，用户无法感知有效切换。
- [x] 4.15 将结果窗口实现为语义明确的译文模式和原图模式：默认显示译文；原图模式完全透明并隐藏译文块；切换按钮显示“显示原图”或“显示译文”；操作栏保持可见并使用 `no-drag` 接收点击。
- [x] 4.16 记录第六次开发环境复测结果：原图/译文状态和按钮文案已经成功切换，原图模式也隐藏了译文块，但结果区域仍显示完整深色矩形，确认剩余问题是结果 `BrowserWindow` 未启用原生透明能力。
- [x] 4.17 为结果 `BrowserWindow` 显式设置 `transparent: true`，保留 `frame: false`、透明背景色、`alwaysOnTop`、`sandbox: false`、`contextIsolation: true` 和共享 preload，并保持操作栏接收鼠标点击。
- [x] 4.18 记录用户对结果浮层行为的澄清：切换只控制原图可见度，显示原图时必须保留半透明背景并看到下方原文，两种状态都必须持续叠加译文；此前原图/译文二选一的实现方向不符合预期。
- [x] 4.19 恢复结果浮层原有叠加行为：两种状态始终渲染译文块；显示原图时使用 `var(--floating-surface)` 半透明背景，隐藏原图时使用 `var(--floating-surface-strong)` 背景；按钮恢复“显示原图/隐藏原图”，并保留原生窗口透明与可点击操作栏。
- [x] 4.20 人工验证显示原图时背景存在、下方原文可见且译文继续叠加，隐藏原图时背景增强且译文保留，以及反复切换、复制原文、复制译文、收藏、Escape 关闭、重复截图、同屏与跨屏、慢速与快速拖拽及启动失败恢复，再完成任务 3.3 中剩余的截图覆盖。

## 5. 打包态 AudioWorklet 回归

- [x] 5.1 记录 Windows x64 打包应用复测结果：口语对话页面和历史记录可以显示，但启动实时对话时提示 `Unable to load a worklet's module.`；确认 Vite 7 将 `pcm-capture-processor.js` 内联为 CSP 不允许的 `data:text/javascript` URL。
- [x] 5.2 将 PCM AudioWorklet 处理器改为 `?url&no-inline` 静态资源导入，把同源构建资源 URL 传给 `audioWorklet.addModule()`，并保持现有处理器逻辑、采集生命周期和严格 CSP 不变。
- [x] 5.3 在获得执行现有 package script 的明确授权后生成生产构建，确认 renderer assets 包含独立 PCM 处理器文件，主 bundle 不再内联对应 `data:text/javascript` URL，且打包应用包含该资源。
- [x] 5.4 人工验证开发环境与 Windows x64 打包应用均可开始、暂停、继续、结束和重新开始实时对话，麦克风采集与语音识别正常且不再出现 worklet 模块加载错误，再完成任务 3.3 中剩余的语音覆盖。

## 6. 划词翻译浮动按钮回归

- [x] 6.1 记录 Windows x64 打包应用复测结果：在学习收藏等页面选中文本后，点击浮动“翻译”按钮会触发 document 级 `pointerup` 重新计算选区并移动按钮，导致浏览器未生成 `click`，右侧翻译抽屉无法打开。
- [x] 6.2 让 document 级 `pointerup` 监听接收事件，并复用 `isIgnoredSelectionNode()` 跳过来自按钮等忽略交互元素的选区更新，保持现有选区定位、滚动与缩放隐藏和抽屉控制逻辑不变。
- [x] 6.3 记录应用 6.2 后的人工复测结果：点击浮动“翻译”按钮时仍会移动到选区下方且抽屉无法打开；确认共享 `Button` 的 `active:translate-y-px` 会在按下阶段覆盖浮动按钮用于上方定位的 `-translate-y-full`，使释放事件在按钮忽略判断前失去原目标。
- [x] 6.4 仅在划词翻译浮动按钮调用处固定 active 纵向位移：位于选区下方时使用 `active:translate-y-0`，位于选区上方时使用 `-translate-y-full active:-translate-y-full`；保留共享 `Button` 的默认按压反馈和现有事件隔离逻辑。
- [x] 6.5 人工验证学习收藏及其他主页面中位于视口顶部、中部、底部的中英文选区：浮动按钮按下和释放时不移动且抽屉正常打开；重复翻译、关闭抽屉、滚动、缩放、Escape 和忽略交互元素保持正常。
- [x] 6.6 复用现有 `tw-animate-css` 和 Radix `data-state`，为划词翻译抽屉内容增加 200ms 右侧滑入与 150ms 右侧滑出动画，并通过 `motion-reduce:animate-none` 尊重系统减少动态效果设置；不新增依赖、定时器或 React 状态。
- [x] 6.7 人工验证抽屉首次打开、关闭按钮、Escape、连续打开与关闭以及翻译加载期间的滑入和滑出动画均流畅且不阻断交互，并确认系统启用减少动态效果时不播放动画。

## 7. 实时对话继续回归

- [x] 7.1 记录 Windows 应用复测结果：实时对话可以暂停并进入 `paused` 状态，但点击“继续”没有响应；确认 `resumeConversation()` 调用 `startListening()` 时状态仍为 `paused`，被后者的入口守卫直接返回。
- [x] 7.2 在 `resumeConversation()` 确认暂停状态后，先通过现有 `updateStatus('initializing')` 同步退出暂停状态，再使用原会话编号调用 `startListening()`，并补全 Hook 依赖；保留消息、上下文、麦克风启动、AudioWorklet、识别 IPC 和错误处理流程。
- [x] 7.3 人工验证开始对话后可以反复暂停与继续，每次继续都经过初始化并恢复麦克风与本地识别，已有消息和上下文不丢失；同时验证继续期间结束、麦克风失败、结束后重新开始及 Windows x64 打包应用行为。

## 8. 划词翻译选区清除回归

- [x] 8.1 记录 Windows 应用复测结果：选中文本后浮动“翻译”按钮正常出现，第一次点击空白区域时浏览器选区已经消失，但按钮仍然停留，再次点击空白区域后才隐藏；确认组件缺少原生选区折叠后的状态同步。
- [x] 8.2 监听 document 级 `selectionchange`，仅在选区不存在、已经折叠或没有 Range 时调用现有隐藏方法清除浮动按钮；有效选区仍由现有 `pointerup` 和 `keyup` 计算位置，并保留按钮 `pointerdown`、忽略交互元素、抽屉和翻译流程。
- [x] 8.3 人工验证选中文本后第一次点击空白区域即可隐藏按钮，并覆盖重新选择、点击翻译按钮、打开和关闭抽屉、Escape、滚动、缩放、键盘选区及忽略交互元素，确认按钮不会提前消失、滞留或在拖选过程中频繁移动。

验证记录：

- Windows x64 未打包应用能够成功启动并完成初始化；用户确认主窗口、截图选择与翻译、多显示器坐标、全局快捷键、通知、剪贴板复制、SQLite 持久化记录、语音识别和语音合成冒烟测试全部通过，任务 3.3 完成。
- 已成功生成并安装 `dist/Bai_Ze-1.0.25-setup.exe`；用户确认应用启动、现有用户数据、更新检查、更新下载进度和重启安装行为全部通过，任务 3.4 完成。
- 当前 Windows 主机没有 Linux Wayland 和已签名 macOS 环境，因此将对应检查记录为未验证，而不是阻塞 Windows 构建。
- Windows 开发测试最初复现了按 F2 后主窗口最小化但截图遮罩不显示的回归。按就绪状态显示的修正已使遮罩出现，但任务 4.4 复测发现拖拽选区和 Escape 取消仍不可用。
- 第二次复测确认遮罩层可以显示，但仍无法开始选区；删除 150ms 显示器跟踪没有改善鼠标输入，并且不适用于多显示器场景。
- 当前保留已完成的焦点处理、Escape 统一取消、跨屏跟随和鼠标命中层。Pointer Capture 与冗余 IPC 清理已实现；第四次复测已能绘制选区，但松开后遮罩层仍然停留，`SCREENSHOT_SELECTED` 未进入主进程后续流程。
- 已恢复每 150ms 的显示器检查，但仅在 `display.id` 变化时移动截图窗口；第一次原生鼠标按下会停止跟踪。截图根容器使用 1% 黑色透明背景确保 Windows 命中，后续实现不再修改原生窗口命中方案。
- 截图选择器现已使用容器 Pointer Capture，Ref 同步保存绘制状态、起点和最新选区，不再等待 Effect 动态挂载移动与松开监听。`START_SCREENSHOT` renderer 发送、主进程监听和枚举均已删除。
- 截图窗口和结果窗口已显式设置 `sandbox: false`，继续使用 `contextIsolation: true` 和共享 preload。第五次复测确认选区完成 IPC、截图捕获、翻译和结果窗口展示流程已经恢复。
- 任务 4.15 已实现明确的原图/译文模式。第六次复测确认按钮文案和译文块可正确切换，但原图模式仍显示深色矩形，因为结果窗口只有透明背景色而未启用原生窗口透明。
- 任务 4.17 已为无边框结果窗口启用 `transparent: true`，使 renderer 半透明背景可以合成下方屏幕内容。
- 用户进一步确认切换只应控制原图可见度，译文必须始终叠加显示；任务 4.15 的原图/译文二选一实现不符合预期。任务 4.19 将恢复原有叠加行为，任务 2.2、3.3 和 4.20 在修正并人工复测后关闭。
- Windows 开发环境人工复测确认截图流程验证成功，任务 4.20 的截图专项覆盖完成；由该回归重新打开的任务 2.2 同步关闭。任务 3.3 仍保留非截图功能的冒烟测试范围。
- Windows x64 打包应用的口语对话页面与历史记录可以正常显示，但启动实时对话时出现 `Unable to load a worklet's module.`。构建产物显示 PCM 处理器被 Vite 7 内联为 `data:text/javascript` URL，与主窗口 `script-src 'self'` CSP 冲突；任务 2.2 因此再次打开，任务 3.3 保持未完成。
- 最新生产构建已输出独立的 `pcm-capture-processor-*.js`，主 bundle 使用同源资源 URL 且不再内联对应 data URL；用户确认开发环境与 Windows x64 打包应用的开始、暂停、继续、结束、重新开始、麦克风采集和语音识别全部正常，未再出现 worklet 模块加载错误，任务 5.4 完成。
- Windows x64 打包应用复测发现，学习收藏等页面的划词翻译浮动按钮会在点击时移动，右侧翻译抽屉无法打开。任务 6.2 已复用既有忽略交互元素判断隔离对应事件，但后续人工复测仍然失败。进一步确认共享 `Button` 的 `active:translate-y-px` 会在按下阶段覆盖浮动按钮的 `-translate-y-full`，使按钮在 `pointerup` 前跳到锚点下方并失去事件目标；任务 6.4 已在浮动按钮调用处按上下位置固定 active 纵向位移，用户复测确认按钮不再移动且抽屉可以打开，任务 6.5 完成。任务 6.6 已为抽屉增加状态驱动的滑入与滑出动画，用户复测确认效果正常，任务 6.7 完成。
- Windows 应用口语对话复测发现，暂停后点击“继续”没有响应。继续按钮绑定正常，但 `resumeConversation()` 在状态仍为 `paused` 时调用 `startListening()`，被后者的暂停守卫直接返回；任务 7.2 已修正状态转换，用户确认暂停、继续及相关功能均正常，任务 7.3 完成人工验证。
- 最新 Windows 应用复测发现，选中文本后第一次点击空白区域虽然已经清除浏览器选区，但浮动“翻译”按钮仍然停留，第二次点击才隐藏。当前组件没有监听原生 `selectionchange`，任务 8.1 完成问题记录，任务 8.2 和 8.3 等待实现与人工复测。
- 任务 8.2 已增加原生 `selectionchange` 同步；用户完成任务 8.3 的全部场景并确认通过，第一次点击空白区域即可隐藏按钮，重新选择、按钮点击、抽屉、Escape、滚动、缩放、键盘选区及忽略交互元素均正常。当前已知 Electron 升级兼容回归均已修复，任务 2.2 关闭。
