## 1. 会话状态与输入流程

- [x] 1.1 在共享对话类型中增加语音、文字输入模式以及文字模式等待输入状态，并补充对应注释。
- [x] 1.2 扩展 `useConversation` 管理当前输入模式和活跃会话期间的切换约束，同时保持现有语音开始、暂停、继续和结束流程。
- [x] 1.3 在共享 AI 请求流程中按模式分发回复后的行为：语音模式朗读并恢复监听，文字模式静音并进入等待输入状态。
- [x] 1.4 增加文字提交操作，统一处理首尾空白、用户消息追加、输入清空、重复提交限制和 AI 回复请求。

## 2. 双模式页面界面

- [x] 2.1 将页面标题和说明调整为覆盖两种输入方式，并使用现有按钮组件实现可访问的语音/文字模式选择控件。
- [x] 2.2 根据当前模式和状态更新状态映射、顶部会话操作及左侧面板标题，确保文字模式不显示暂停、继续等语音专属操作。
- [x] 2.3 使用现有文本域和按钮组件在左侧交互区实现文字输入界面，支持 Enter 发送、Shift+Enter 换行、空白拦截以及处理期间禁用。
- [x] 2.4 保持右侧对话记录组件复用，并检查两种模式下消息、中文释义和表达纠正的展示一致性。

## 3. 行为检查

- [x] 3.1 检查默认语音模式的开场、朗读、监听、暂停、继续、结束和错误重试行为未发生变化。
- [x] 3.2 检查文字模式的开场静音、文字发送、快捷键、重复提交限制、结束后记录保留和重新开始行为符合规格。
- [x] 3.3 检查活跃会话模式锁定、空闲模式切换以及响应式布局和键盘可访问状态符合设计。

## 4. 页面层级与滚动修订

- [x] 4.1 将侧边导航、模式选项、左侧面板标题和空闲主操作统一为“对话练习 / 语音练习 / 文字练习 / 开始练习”的文案层级，保持路由和内部模式类型不变。
- [x] 4.2 在桌面端建立从对话页面到双栏工作区和右侧记录列的受约束高度链，使对话记录独立滚动，并移除易失准的视口高度计算。
- [x] 4.3 保持窄屏上下布局的整页自然滚动，避免在移动尺寸形成受限高度的嵌套记录滚动区。
- [x] 4.4 移除语音空闲状态中央图标的开始点击行为，仅保留页头主操作，并核对两种模式下开始、重新开始和运行中控制的可访问状态。

## 5. 文字模式 AI 回复朗读

- [x] 5.1 为文字模式增加默认关闭、仅在当前页面生命周期内保留的 AI 回复朗读状态及切换操作。
- [x] 5.2 扩展 AI 回复后的行为分发：文字朗读开启时复用现有语音合成并在完成后等待输入，关闭时直接等待输入，且两条路径均不启动麦克风或语音识别。
- [x] 5.3 在文字练习界面增加可访问的“朗读 AI 回复”开关，并确保该控件不在语音练习中显示。
- [x] 5.4 检查默认静音、开启后朗读、关闭后静音、朗读失败后继续输入、离开页面后恢复默认值以及语音模式行为不变。
