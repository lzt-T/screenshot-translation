## 1. 建立开场灵感与请求数据

- [x] 1.1 在共享对话模块定义稳定的开场灵感键、指导语映射和近期记录类型，保持指导语为生成方向而非固定英文问题
- [x] 1.2 扩展开场请求数据，使其携带选中的灵感指导、近期开场文本和可选的明确冲突文本，非开场请求保持现有数据范围
- [x] 1.3 精简新会话提示词的负向限制，并将当前灵感与最近 12 条开场作为正向生成上下文

## 2. 跨页面保存近期开场

- [x] 2.1 使用现有 `localforage` 读取独立的近期开场记录，缺失或无效数据按空列表处理
- [x] 2.2 在最终采用开场后保存英文文本和灵感键，并按时间顺序只保留最新 12 条
- [x] 2.3 移除被持久化记录替代的页面内开场引用和相关孤儿常量、类型或参数

## 3. 选择灵感并限制重复

- [x] 3.1 从近期未使用的灵感中随机选择当前方向，全部用过时回退到完整灵感集合
- [x] 3.2 增加忽略大小写、标点和连续空白差异的开场规范化比较函数
- [x] 3.3 首次结果命中近期文本时更换灵感并额外请求一次，确保重试结果直接结束生成且不会形成循环
- [x] 3.4 只对新会话开场执行选择、比较和重试，保持用户语音或文字回复后的现有请求流程不变

## 4. 完成静态核对

- [x] 4.1 核对离开页面后重新进入以及应用重启后的近期记录读取、追加和上限淘汰路径
- [x] 4.2 核对正常开场只请求一次、首次明显重复最多请求两次，并且失败重试仍使用同一开场请求信息
- [x] 4.3 核对语音练习、文字练习、朗读偏好、连续对话上下文和纠错展示未被本次变更修改
